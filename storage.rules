rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Report images: public read, auth write
    match /reports/{imageId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

